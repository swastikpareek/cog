fonts:
  version: VERSION
  css:
    theme:
      '//fonts.googleapis.com/css?family=Open+Sans:400,700,300': { type: external} # external font
      fonts/signika.css: {} # local font
global:
  version: VERSION
  css:
    theme:
      css/styles.css: {}